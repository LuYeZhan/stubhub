export enum Urls {
  CATEGORIES = 'categories',
  EVENTS = 'events',
  PURCHASES = 'purchases',
  TICKETS = 'tickets',
  USERS = 'users',
}

export enum DetailsUrls {
  CATEGORIES = 'categories',
  USERS = 'users',
}

export enum IdUrls {
  EVENTS = 'events',
  PURCHASES = 'purchases',
}

export enum IdTypes {
  USERID = 'userid',
  CATEOGORY_ID = 'categoryid',
}