export enum URLS {
  CATEGORIES = 'categories',
  EVENTS = 'events',
  PURCHASES = 'purchases',
  TICKETS = 'tickets',
  USERS = 'users',
}

export enum DETAILS_URLS {
  CATEGORIES = 'categories',
  USERS = 'users',
}

export enum ID_URLS {
  EVENTS = 'events',
  PURCHASES = 'purchases',
}

export enum ID_TYPES {
  USERID = 'userid',
  CATEOGORY_ID = 'categoryid',
  EVENT_ID = 'eventId',
  SELLER_ID = 'sellerId'
}